import styled from 'styled-components/native';

export const Wrapper = styled.SafeAreaView`
    background: #fff;
    flex: 1;
    justify-content: center;
    align-items: center;
`;

export const ButtonReturn = styled.Image`
   width: 7px;
   height: 14px;
`;
